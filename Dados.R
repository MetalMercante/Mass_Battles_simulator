######################################################################
######################## DADOS #######################################
######################################################################

### D20 ###
d20 <- function(n=1,bonus=0){
        a<-NULL
        for (i in 1:n) a[i] <- sample(1:20,1) +bonus
        a
}

### D12 ###
d12 <- function(n=1,bonus=0){
        a<-NULL
        for (i in 1:n) a[i] <- sample(1:12,1) +bonus
        a
}

### D10 ###
d10 <- function(n=1,bonus=0){
        a<-NULL
        for (i in 1:n) a[i] <- sample(1:10,1) +bonus
        a
}

### D8 ###
d8 <- function(n=1,bonus=0){
        a<-NULL
        for (i in 1:n) a[i] <- sample(1:8,1) +bonus
        a
}

### D6 ###
d6 <- function(n=1,bonus=0){
        a<-NULL
        for (i in 1:n) a[i] <- sample(1:6,1) +bonus
        a
}

### D4 ###
d4 <- function(n=1,bonus=0){
        a<-NULL
        for (i in 1:n) a[i] <- sample(1:4,1) +bonus
        a
}

######################################################################
######################################################################
######################################################################